jsPsych.plugins["spatial-orientation"] = (() => {
    const plugin = {};

    plugin.info = {
        name: "spatial-orientation",
        parameters: {},
    };

    plugin.trial = (display_element, trial) => {};

    return plugin;
})();
